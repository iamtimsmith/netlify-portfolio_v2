import { BreakPoints, Colors, ContainerWidth, Typography, Utilities } from './Variables';

const theme = {
  BreakPoints,
  Colors,
  ContainerWidth,
  Typography,
  Utilities,
  Primary: "#222"
}

export default theme